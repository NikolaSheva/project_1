## Команда: `load_initial_data`

Загружает начальные данные из фикстур (`auth.json`, `one_watch.json`) только если они были изменены.

### Как работает:
1. Вычисляет хеш-функцию (`SHA256`) от содержимого файлов `auth.json`, `one_watch.json`.
2. Сравнивает с сохранённым хешем в `.initial_data_hash`.
3. Если хеш совпадает — загрузка пропускается.
4. Если отличается или файл отсутствует — выполняется `loaddata`, и хеш обновляется.

### Пример использования:
```bash
python manage.py load_initial_data